const me = {
    name : "박미현",
    age : 24,
};

//const name = me.name;
//const age = me.age; 와 동일
const {name, age} = me;

//const a = arr[0]; 과 const [a] = arr; 는 동일
console.log(name);
console.log(name);
console.log(name);